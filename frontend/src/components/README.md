# components/

Shared, reusable, presentation-only UI building blocks (buttons, inputs,
cards, modals, etc.) with no business logic. Organize by primitive type
under `ui/` as the design system grows.
