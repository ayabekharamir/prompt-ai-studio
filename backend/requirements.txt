# =========================================================
# Prompt AI Studio - Backend Dependencies
# =========================================================

# --- Core Framework ---
fastapi==0.115.0
uvicorn[standard]==0.32.0

# --- Database / ORM ---
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9
asyncpg==0.29.0

# --- Validation / Settings ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0  # required by Pydantic's EmailStr (used in schemas/auth.py, schemas/user.py)

# --- Auth / Security ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0
python-multipart==0.0.12

# --- HTTP client (for future SMS / AI provider calls) ---
httpx==0.27.2

# --- Utilities ---
python-dotenv==1.0.1

# --- Dev / Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
black==24.10.0
ruff==0.6.9
